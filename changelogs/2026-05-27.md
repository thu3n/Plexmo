# Changelog - 2026-05-27

- **Reworked `CLAUDE.md` into a prescriptive agent guide.** Adopted structured rules from the OrbitJumper project's CLAUDE.md (agent behavior, coding standards, mandatory post-task checklist, security, QA & tools) while excluding orbit-specific content that doesn't apply to Plexmo (HTML5-canvas UI rules, the `G` state singleton, the season-cutover release cadence, mobile/Supabase guardrails). Cross-references `.agent/rules/nospagetti.md` as the authoritative architecture source rather than duplicating it. _Affected: `CLAUDE.md`._ Category: `chore`
- **Introduced the `changelogs/` directory.** Established a dated change-log convention (`changelogs/YYYY-MM-DD.md`, one entry per task) as the primary record for backtracking changes, since git commit messages alone were too terse to reconstruct history. Added it to the directory layout and made a changelog entry a mandatory post-task checklist item in `CLAUDE.md`. _Affected: `changelogs/2026-05-27.md`, `CLAUDE.md`._ Category: `chore`
- **Decomposed the three 900+ line files (backend rehaul #4).** Pure mechanical extraction with zero behavior change; every original import path (`@/lib/rules`, `@/lib/plex`, the RuleModal path) still resolves so no call site needed editing. (1) `src/lib/rules.ts` (905 lines) → `src/lib/rules/` folder: `rules-crud.ts`, `rules-assignments.ts`, `rules-enforcement.ts`, `rules-schedule.ts`, `types.ts`, and an `index.ts` barrel. (2) `src/lib/plex.ts` (922 lines) → `src/lib/plex/` folder: `plex-types.ts`, `plex-client.ts`, `plex-sessions.ts`, `plex-library.ts`, `plex-users.ts`, and an `index.ts` barrel (the library↔sessions ESM cycle verified to resolve at runtime). (3) `src/features/rules/components/settings/RuleModal.tsx` (929 → 402 lines) → shell + presentational children under `rule-forms/`: `ScheduledAccessForm.tsx`, `EnforcementForm.tsx`, `NotificationsTab.tsx`, `ScopeTab.tsx`, `SaveConfirmationDialog.tsx`. All resulting files are under ~500 lines. Verified: `tsc` clean (only the known stale `.next/types` errors), `vitest` green, dev server compiles `/settings/rules` and `/api/dashboard`. Incidental cleanup along the way: dropped a dead `sendDiscordNotification` import and an unused `syncLibraries` destructure, changed two never-reassigned `let`s to `const`, and hoisted the duplicated Discord SVG + weekday array to named constants. _Affected: `src/lib/rules/*`, `src/lib/plex/*`, `src/features/rules/components/settings/RuleModal.tsx`, `src/features/rules/components/settings/rule-forms/*`._ Category: `refactor`
