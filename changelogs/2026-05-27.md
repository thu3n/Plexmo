# Changelog - 2026-05-27

- **Reworked `CLAUDE.md` into a prescriptive agent guide.** Adopted structured rules from the OrbitJumper project's CLAUDE.md (agent behavior, coding standards, mandatory post-task checklist, security, QA & tools) while excluding orbit-specific content that doesn't apply to Plexmo (HTML5-canvas UI rules, the `G` state singleton, the season-cutover release cadence, mobile/Supabase guardrails). Cross-references `.agent/rules/nospagetti.md` as the authoritative architecture source rather than duplicating it. _Affected: `CLAUDE.md`._ Category: `chore`
- **Introduced the `changelogs/` directory.** Established a dated change-log convention (`changelogs/YYYY-MM-DD.md`, one entry per task) as the primary record for backtracking changes, since git commit messages alone were too terse to reconstruct history. Added it to the directory layout and made a changelog entry a mandatory post-task checklist item in `CLAUDE.md`. _Affected: `changelogs/2026-05-27.md`, `CLAUDE.md`._ Category: `chore`
- **Decomposed the three 900+ line files (backend rehaul #4).** Pure mechanical extraction with zero behavior change; every original import path (`@/lib/rules`, `@/lib/plex`, the RuleModal path) still resolves so no call site needed editing. (1) `src/lib/rules.ts` (905 lines) → `src/lib/rules/` folder: `rules-crud.ts`, `rules-assignments.ts`, `rules-enforcement.ts`, `rules-schedule.ts`, `types.ts`, and an `index.ts` barrel. (2) `src/lib/plex.ts` (922 lines) → `src/lib/plex/` folder: `plex-types.ts`, `plex-client.ts`, `plex-sessions.ts`, `plex-library.ts`, `plex-users.ts`, and an `index.ts` barrel (the library↔sessions ESM cycle verified to resolve at runtime). (3) `src/features/rules/components/settings/RuleModal.tsx` (929 → 402 lines) → shell + presentational children under `rule-forms/`: `ScheduledAccessForm.tsx`, `EnforcementForm.tsx`, `NotificationsTab.tsx`, `ScopeTab.tsx`, `SaveConfirmationDialog.tsx`. All resulting files are under ~500 lines. Verified: `tsc` clean (only the known stale `.next/types` errors), `vitest` green, dev server compiles `/settings/rules` and `/api/dashboard`. Modal manually verified in-app (create/edit/save for every rule type). Incidental cleanup along the way: dropped a dead `sendDiscordNotification` import and an unused `syncLibraries` destructure, changed two never-reassigned `let`s to `const`, and hoisted the duplicated Discord SVG + weekday array to named constants. _Affected: `src/lib/rules/*`, `src/lib/plex/*`, `src/features/rules/components/settings/RuleModal.tsx`, `src/features/rules/components/settings/rule-forms/*`._ Category: `refactor`
- **Typed `user_stats.ts` aggregate queries (refactor #2 follow-up).** Replaced the 4 `as any` casts on the period/all-time SUM/COUNT query results with a `PeriodStatsRow` projection interface (`{ count: number; duration: number | null }`) and typed `StatsParams`/`PeriodStatsParams` bind params via the `db.prepare<Params, Row>()` generic. `SUM()` is NULL on empty sets, so `duration` is nullable and the call sites' `|| 0` coalescing is preserved (now via optional chaining since `.get()` is `Row | undefined`). _Affected: `src/lib/user_stats.ts`._ Category: `refactor`
- **Decomposed the three secondary oversized files (backend rehaul #4, secondary targets).** Zero behavior change. (1) `tautulli/api/route.ts` (668 → 35 lines, now HTTP-only) → the whole background import moved into `src/lib/tautulli-import.ts` (`runTautulliImport`) plus stateless API helpers in `src/lib/tautulli-client.ts` (`resolveServerNames`, `fetchActiveSessions`, `fetchServerCount`, `fetchSeriesMeta`, `fetchMovieMeta`, pure `buildCompatibleEntry`); dropped a dead `cappedCount`, two comment-only empty `if`s, and a no-op eslint-disable. (2) `settings/import/page.tsx` (666 → 328) → shell + `ServerMappingStep`/`ImportProgressStep`/`ImportStatsModal` + shared `types.ts` under `src/features/settings/components/import/`; dropped the dead `selectSourceServer`/`targetServerId` (the unreachable `mapping` step). (3) `setup/page.tsx` (571 → 355) → shell + `LoginStep`/`ServerConfigStep` under `src/features/setup/components/`; dropped an unused `useEffect` import. All resulting files under ~500 lines. Verified: `tsc` clean, `vitest` green, module graph resolves at runtime, and all three routes compile on the dev server. **Note (possible pre-existing bug, left untouched per refactor rules):** the import page renders two stats modals simultaneously when `showDetails` is true — an inline one (in `ImportProgressStep`) and a portal one (`ImportStatsModal`, `z-[9999]`) that visually shadows it. _Affected: `src/app/api/settings/import/tautulli/api/route.ts`, `src/lib/tautulli-import.ts`, `src/lib/tautulli-client.ts`, `src/app/settings/import/page.tsx`, `src/features/settings/components/import/*`, `src/app/setup/page.tsx`, `src/features/setup/components/*`._ Category: `refactor`
